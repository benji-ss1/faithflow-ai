# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/faithflow

# Auth.js
AUTH_SECRET=change-me-run-openssl-rand-base64-32
AUTH_URL=http://localhost:3000

# S3 (or S3-compatible: MinIO, R2, Supabase Storage S3)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=faithflow-media
S3_ENDPOINT=

# Public app URL (used in outbound emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Streaming STT
DEEPGRAM_API_KEY=
NEXT_PUBLIC_AUDIO_WS_URL=ws://localhost:3001

# Summarization LLM (xAI, OpenAI-compatible)
XAI_API_KEY=
XAI_BASE_URL=https://api.x.ai/v1
XAI_MODEL=grok-2-latest

# OpenFlow AI assistant — its OWN Groq key, billed/rate-limited independently of
# the shared GROQ_API_KEY used by verse/song detection. Leave unset to keep
# OpenFlow switched off (the panel degrades gracefully with a clear message).
OPENFLOW_GROQ_API_KEY=
# Optional model override for OpenFlow (default: llama-3.3-70b-versatile).
OPENFLOW_GROQ_MODEL=
# Public flag that shows the OpenFlow entry in the desktop shell. Set to 1 ONLY
# where OPENFLOW_GROQ_API_KEY is also set, so the entry never appears dead.
NEXT_PUBLIC_OPENFLOW_ENABLED=

# Email delivery (Phase 5)
# Leave RESEND_API_KEY unset in dev — verification / reset links will print
# to the terminal instead of being sent.
RESEND_API_KEY=
EMAIL_FROM=FaithFlow AI <no-reply@faithflow.ai>

# Stripe (Phase 5, test mode ONLY)
# Must be an sk_test_ key. The client refuses live keys until billing is
# explicitly enabled.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=
