// CNDI.h — umbrella for the NDI SDK C API, imported into Swift as `import CNDI`.
//
// "Processing.NDI.Lib.h" and its siblings are copied here from the installed
// NDI SDK for Apple by native/macos/build.sh (they are license-gated and not
// committed). If this include fails, you have not run build.sh's header-copy
// step — install the NDI SDK for Apple and re-run native/macos/build.sh.
#ifndef PRESENTFLOW_CNDI_H
#define PRESENTFLOW_CNDI_H

#include "Processing.NDI.Lib.h"

#endif /* PRESENTFLOW_CNDI_H */
